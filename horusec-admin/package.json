{
  "name": "horusec-admin",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && nodemon -e js,ejs,ts,tsx,json dist/app.js",
    "serve": "npm run build && node dist/app.js",
    "lint": "npx tslint --project tsconfig.json",
    "lint-fix": "npx tslint --project tsconfig.json --fix",
    "build": "npx tsc"
  },
  "author": "ZupIT",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.11",
    "nodemon": "^2.0.7",
    "tslint": "^6.1.3",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "crypto": "^1.0.1",
    "ejs": "^3.1.5",
    "express": "^4.17.1",
    "pg": "^8.5.1",
    "pg-hstore": "^2.3.3",
    "sequelize": "^6.4.0"
  }
}
